<base:ZtControlBase
    x:Class="ZtClientFramework.ZtLabel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:ZtClientFramework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">
    <StackPanel
        x:Name="ControlParent"
        Orientation="Horizontal"
        Visibility="{x:Bind Visibility, Mode=TwoWay}">
        <TextBlock
            x:Name="ContentControl"
            Style="{x:Bind Style, Mode=TwoWay}"
            IsTextSelectionEnabled="True"
            Text="{x:Bind Text, Mode=TwoWay}"
            TextWrapping="{x:Bind TextWrapping, Mode=TwoWay}" />
    </StackPanel>
</base:ZtControlBase>